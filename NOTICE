ServoLoop third-party notices

ServoLoop links unravel-agent-runtime and unravel-agent-providers from
https://github.com/unravelaidk/unravel-agent-runtime under AGPL-3.0-only.
Their source revision is pinned in Cargo.toml and Cargo.lock. The original
provider attribution is retained below for binary distributions.

# NOTICE — unravel-agent-providers

This crate adapts patterns from the Khadim project (`arvel-ai-core`),
which is licensed under the GNU Affero General Public License v3.0.

The shared implementation was extracted from ServoLoop's `servoloop-providers`
crate (https://github.com/unravelaidk/servoloop). The original attribution and
modification history below are retained.

## Source

- Repository: https://github.com/unravel-ai/khadim
- Original files consulted:
  - `crates/arvel-ai-core/src/models.rs` — Models.dev catalog fetch/parse,
    provider-specific model discovery (NVIDIA, OpenRouter, Ollama).
  - `crates/arvel-ai-core/src/providers/openai_completions.rs` —
    OpenAI-compatible chat completions request building, streaming SSE
    parsing, tool-call assembly.
  - `crates/arvel-ai-core/src/providers/transform_messages.rs` —
    canonical-to-OpenAI message conversion, image content, tool-call ID
    normalization.
  - `crates/arvel-ai-core/src/env_api_keys.rs` — environment-based
    provider endpoint and API key resolution.
  - `crates/arvel-ai-core/src/streaming.rs` — SSE event line splitting.

## Modifications

The adapted code was substantially rewritten to fit the ServoLoop core
contracts (`servoloop-core::model::Model`, `Message`, `ToolCall`,
`StreamDelta`, `Usage`, `ModelError`):

- No giant static model ID catalog; discovery is fully remote.
- Instance-owned TTL caches keyed by endpoint + account identity (no
  process-global `OnceLock<Mutex<…>>`).
- Secrets are never stored in cache keys or emitted in Debug/log output.
- Models.dev parsing preserves provenance and capability uncertainty
  (unknown capability is never silently upgraded to supported).
- `ProviderSpec` typed protocol/endpoint/env/keyless-policy selection.
- Credential redaction in all error paths.
- reqwest with rustls, no redirects, bounded response/SSE limits.

The extraction renamed the public runtime and provider crates to
`unravel-agent-runtime` and `unravel-agent-providers`. The shared Chat Completions
transport now also supports application-owned headers and raw argument strings,
while canonical model calls retain strict validation. Application branding and
legacy argument repair remain in the consumers.