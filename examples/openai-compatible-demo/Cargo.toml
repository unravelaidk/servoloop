[package]
name = "servoloop-openai-compatible-demo"
description = "Live model demo for the servoloop-providers OpenAI-compatible adapter"
publish = false
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
async-trait.workspace = true
servoloop-core = { path = "../../crates/servoloop-core" }
servoloop-providers = { path = "../../crates/servoloop-providers" }
tokio.workspace = true